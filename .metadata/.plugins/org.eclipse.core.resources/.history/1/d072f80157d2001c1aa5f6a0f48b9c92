package study;
/*if문 활용을 해보자
 */
import java.util.*;
public class D002IF {

	public static void main(String[] args) {
	// 스캐너를 받아서 어제 home work 2처럼 두 정수와 연산자를 입력받아보자 
		Scanner sc= new Scanner(System.in);
	/*int a,b;
	char op;
	System.out.print("첫번째 정수 입력"); // 첫번째 정수를 입력 12345
	a = sc.nextInt();
	
	System.out.print("연산자 입력"); // 연산자 입력 +
	op= sc.next().charAt(0); 
			//sc.next();은 문자열이기때문에  sc.next().을 치면 
			//index 처음 문자만 가져온다는 charAt(0)을써준다.
			// 만약 string으로 간다면 sc.next();만 써주면 된다. 
			//char은 문자 하나기때문에 charAt(0) -- 문자 하나만 가져온다!
	System.out.print("두번째 정수 입력"); // 두번째 정수 입력 4567564
	b= sc.nextInt();
	*/
		
	// 정수를 입력받아 홀수 인지 짝수인지 판정하세요~ if문을 사용하세요~
		
		int v;
		
		System.out.println("첫번째 정수 입력");
		v= sc.nextInt();
		if (v%2==0)
			{System.out.println("짝수");
			System.out.println("짝수군요");}
		// 만약 홀수를 집어 넣는다면 "짝수"라는 문장은 출력되지 않는다.
		// 블록이 없다면 바로 밑에것만 인식	
		// 범위안에 있는 거 다  인식!!!!!
		
	}

}
